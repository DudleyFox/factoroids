import GameScreenBase from './GameScreenBase';

export class GameLevel extends GameScreenBase {
    constructor(level, keyHandler, ub) {
        this.level = level;
        this.ship = ship;
        this.lives = lives;
    }

    update(delta) {

    }

    draw(context) {

    }

    setUpperBounds(ub) {

    }
}